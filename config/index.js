const database = require('./database.config');
const invoice = require('./invoice.config');
const paths = require('./paths');
const server = require('./server.config');

module.exports = {
    database,
    invoice,
    paths,
    server
};